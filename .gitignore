node_modules/
dist/
build/
.env
.env.local
!.env.example
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
coverage/
*.tsbuildinfo
mcps/
terminals/
server/.env
.vercel
.vercel

# scratch downloads (agent debugging)
/*.html
!client/**/*.html
/hunts.json

# Android TWA signing keystore — NEVER commit (dapp-store signing secret)
*.keystore
android-twa/*.keystore

# Solana keypair files — NEVER commit (wallet secret keys, devnet or mainnet)
*-authority.json
*-keypair.json
devnet-*.json

# Local scratch scripts (AP tooling)
.ap/
